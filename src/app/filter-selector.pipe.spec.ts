import { FilterSelectorPipe } from './filter-selector.pipe';

describe('FilterSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
